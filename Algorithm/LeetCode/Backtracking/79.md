    bool is_inside(int x, int y, int rowsize, int colsize){
        return(0<=x&&x<=rowsize && 0<=y&&y<=colsize);
    }
    out of index 오류 발생해서 체크하려고 만든 함수
    그런데 x<=rowsize로 x=rowsize가 되어도 true를 반환해 out of index가 is_inside함수 내에서 발생



    두 가지 포인트를 짚어볼게요:

첫 번째 포인트 (방문 체크의 위치):
exist 함수에서 dfs(i, j, 0)를 부르기 전에 visited[i][j] = 1로 먼저 체크를 하셨죠? 그런데 dfs 함수가 시작하자마자 첫 줄에서 하는 일은 무엇인가요?


if(visited[posx][posy] || idx > word_.length()){
    return;
}
시작하자마자 visited가 1인지 검사하기 때문에, 출발하자마자 return 되어버려서 아래쪽 코드가 아예 실행되지 않고 있습니다!

두 번째 포인트 (글자 일치 검사):
우리가 찾으려는 글자와 보드의 글자가 같은지는 언제 확인해야 할까요?
마지막에 한꺼번에 확인하는 것보다, 새로운 칸에 들어왔을 때 지금 그 칸의 글자가 내가 찾던 글자(word_[idx])와 일치하는지 먼저 확인하는 것이 훨씬 안전하고 직관적입니다.

이 두 가지를 염두에 두고 흐름을 다시 한번 따라가 보세요! 거의 다 오셨습니다.


